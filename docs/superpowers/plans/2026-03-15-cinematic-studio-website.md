# Wise Goose Games — Cinematic Studio Website Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rebuild the Wise Goose Games website from static HTML to Astro + React Islands with full SEO, GSAP animations, dark/light theming, and a modular game catalog.

**Architecture:** Astro generates static HTML pages for SEO. React components hydrate as islands for interactive features (GSAP animations, theme toggle, contact form, lightbox). Game catalog is a TypeScript data module — pages auto-generate from it.

**Tech Stack:** Astro 5.x, React 19, Tailwind CSS 3.4.x, GSAP 3 + ScrollTrigger, Lucide React, Formspree, GitHub Actions

**Spec:** `docs/superpowers/specs/2026-03-15-cinematic-studio-website-design.md`

---

## Chunk 1: Project Scaffolding & Foundation

### Task 1: Initialize Astro Project

**Files:**
- Create: `package.json`
- Create: `astro.config.mjs`
- Create: `tailwind.config.mjs`
- Create: `tsconfig.json`
- Create: `.gitignore`

- [ ] **Step 1: Initialize npm and install dependencies**

```bash
cd /Users/tbarr/Documents/GitHub/wise-goose-games.github.io
npm init -y
npm install astro@latest @astrojs/react @astrojs/tailwind @astrojs/sitemap react@19 react-dom@19 tailwindcss@3.4 gsap@3 lucide-react
npm install -D @types/react @types/react-dom typescript
```

- [ ] **Step 2: Create astro.config.mjs**

```javascript
// astro.config.mjs
import { defineConfig } from 'astro/config';
import react from '@astrojs/react';
import tailwind from '@astrojs/tailwind';
import sitemap from '@astrojs/sitemap';

export default defineConfig({
  site: 'https://wisegoosegames.com',
  integrations: [react(), tailwind(), sitemap()],
});
```

- [ ] **Step 3: Create tailwind.config.mjs**

```javascript
// tailwind.config.mjs
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{astro,html,js,jsx,ts,tsx}'],
  theme: {
    extend: {
      colors: {
        bg: 'var(--bg-color)',
        text: 'var(--text-color)',
        accent: 'var(--accent-color)',
        'accent-hover': 'var(--accent-hover)',
        'light-gray': 'var(--light-gray)',
        border: 'var(--border-color)',
        surface: 'var(--surface-color)',
        'deep-dark': 'var(--deep-dark)',
      },
      fontFamily: {
        body: ['Montserrat', 'sans-serif'],
        drama: ['Cormorant Garamond', 'serif'],
        mono: ['IBM Plex Mono', 'monospace'],
      },
      borderRadius: {
        '4xl': '2rem',
      },
    },
  },
  plugins: [],
};
```

- [ ] **Step 4: Create tsconfig.json**

```json
{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "jsx": "react-jsx",
    "jsxImportSource": "react"
  }
}
```

- [ ] **Step 5: Update .gitignore**

```
node_modules/
dist/
.astro/
.DS_Store
```

- [ ] **Step 6: Add npm scripts to package.json**

Update `package.json` scripts:
```json
{
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  }
}
```

- [ ] **Step 7: Verify build runs**

```bash
npm run build
```

Expected: Astro builds successfully with an empty project (no pages yet is OK, just verify no config errors).

- [ ] **Step 8: Commit**

```bash
git add package.json package-lock.json astro.config.mjs tailwind.config.mjs tsconfig.json .gitignore
git commit -m "feat: initialize Astro project with React, Tailwind, GSAP"
```

---

### Task 2: Migrate Static Assets to public/

**Files:**
- Create: `public/` directory with all migrated assets
- Move: logos, favicons, CNAME, robots.txt, etc.

- [ ] **Step 1: Create public directory structure and move assets**

```bash
mkdir -p public/.well-known

# Copy assets to public/ (keep originals until old HTML is removed)
cp WWG_Goose_Header_280px.png public/
cp WWG_Text_Header_280px.png public/
cp WWG_Text_Header_Dark_280px.png public/
cp favicon.ico public/
cp favicon.png public/
cp favicon.svg public/
cp apple-touch-icon-180x180.png public/
cp og-hero-logo-1200x630.png public/
cp logo_512x512.png public/
cp robots.txt public/
cp app-ads.txt public/
cp pinterest-8e7d0.html public/
cp .nojekyll public/
cp CNAME public/
cp .well-known/atproto-did public/.well-known/atproto-did
```

- [ ] **Step 2: Remove old sitemap.xml from root** (will be generated by @astrojs/sitemap)

```bash
# Only if it exists at root — do NOT put it in public/
rm -f sitemap.xml
```

- [ ] **Step 3: Verify public/ contains all required assets**

```bash
ls -la public/
ls -la public/.well-known/
```

Expected: All files listed in spec's project structure are present.

- [ ] **Step 4: Commit**

```bash
git add public/
git commit -m "feat: migrate static assets to public/ for Astro build"
```

---

### Task 3: Create Global CSS with Brand Palette

**Files:**
- Create: `src/styles/global.css`

- [ ] **Step 1: Create global.css with CSS variables, noise overlay, base styles**

```css
/* src/styles/global.css */

/* ===== Light Mode (default) ===== */
:root {
  --bg-color: #FDFCF8;
  --text-color: #2C2C2C;
  --accent-color: #E87A2E;
  --accent-hover: #D06C28;
  --light-gray: #F0F0F0;
  --border-color: #E0E0E0;
  --surface-color: #FFFFFF;
  --deep-dark: #2C2C2C;
}

/* ===== Dark Mode ===== */
[data-theme="dark"] {
  --bg-color: #1C1612;
  --text-color: #E0E0E0;
  --accent-color: #E87A2E;
  --accent-hover: #D06C28;
  --light-gray: #2D2621;
  --border-color: #3A342E;
  --surface-color: #24201C;
  --deep-dark: #0F0D0A;
}

/* ===== Base Reset ===== */
*,
*::before,
*::after {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  font-family: 'Montserrat', sans-serif;
  background-color: var(--bg-color);
  color: var(--text-color);
  margin: 0;
  line-height: 1.6;
  min-height: 100vh;
  transition: background-color 0.3s ease, color 0.3s ease;
}

/* ===== Noise Overlay ===== */
body::after {
  content: '';
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: 9999;
  opacity: 0.05;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
  background-repeat: repeat;
  background-size: 256px 256px;
}

/* ===== Logo Theme Switching ===== */
.logo-text-light {
  display: block;
}
.logo-text-dark {
  display: none;
}
[data-theme="dark"] .logo-text-light {
  display: none;
}
[data-theme="dark"] .logo-text-dark {
  display: block;
}

/* ===== Button Base ===== */
.btn-accent {
  position: relative;
  overflow: hidden;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  padding: 0.875rem 2rem;
  background-color: var(--accent-color);
  color: white;
  font-family: 'Montserrat', sans-serif;
  font-weight: 600;
  font-size: 1rem;
  border: none;
  border-radius: 0.5rem;
  cursor: pointer;
  text-decoration: none;
  transition: transform 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94),
              box-shadow 0.3s ease;
  box-shadow: 0 4px 12px rgba(232, 122, 46, 0.3);
  z-index: 1;
}

.btn-accent:hover {
  transform: scale(1.03);
  box-shadow: 0 6px 20px rgba(232, 122, 46, 0.4);
}

.btn-accent .bg-slide {
  position: absolute;
  inset: 0;
  background-color: var(--accent-hover);
  transform: translateX(-100%);
  transition: transform 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  z-index: -1;
}

.btn-accent:hover .bg-slide {
  transform: translateX(0);
}

/* ===== Link Hover ===== */
a {
  transition: transform 0.2s ease, color 0.2s ease;
}
a:hover {
  transform: translateY(-1px);
}

/* ===== Focus Rings (Accessibility) ===== */
:focus-visible {
  outline: 2px solid var(--accent-color);
  outline-offset: 2px;
}

/* ===== Status Dot Animation ===== */
@keyframes pulse-dot {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

.status-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background-color: #22c55e;
  animation: pulse-dot 2s ease-in-out infinite;
}

/* ===== Reduced Motion ===== */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add src/styles/global.css
git commit -m "feat: add global CSS with brand palette, noise overlay, dark mode"
```

---

### Task 4: Create Game Catalog Data Module

**Files:**
- Create: `src/data/games.ts`

- [ ] **Step 1: Create the typed game catalog**

```typescript
// src/data/games.ts

export type GameStatus = "unannounced" | "announced" | "coming-soon" | "released";

export interface GamePlatform {
  url: string;
  appId?: string;
}

export interface FeatureHighlight {
  icon: string;
  title: string;
  description: string;
}

export interface Game {
  id: string;
  title: string;
  status: GameStatus;
  releaseDate: string | null;
  price: string | null;
  genre: string;
  tagline: string;
  shortDescription: string;
  longDescription: string;
  features: string[];
  platforms: Record<string, GamePlatform>;
  trailer: string | null;
  steamAssets: {
    header?: string;
    screenshots?: string[];
  };
  pressKit: string | null;
  tags: string[];
  featureHighlights: FeatureHighlight[];
  keyMechanics: {
    core?: string;
    pieces?: string;
    progression?: string;
  };
  audience: {
    primary?: string;
    positioning?: string;
  };
}

export const games: Game[] = [
  {
    id: "pathways-poltergeists",
    title: "Pathways & Poltergeists",
    status: "released",
    releaseDate: "February 27, 2026",
    price: "$9.99 USD",
    genre: "Spatial Puzzle",
    tagline: "Looks Cozy. Plays Hard.",
    shortDescription: "Guide ghost friends home by building pathways from polyomino pieces. A cozy-looking spatial puzzle game that will absolutely break your brain.",
    longDescription: "Four ghost friends are lost in the fog. Build pathways from 3D polyomino pieces to guide them home. Place, rotate, and connect modular pathway pieces to navigate each ghost to their exit. What starts simple quickly evolves with obstacles blocking your path, waypoints requiring connections, teleporters that break the space, and multiple ghosts that need to move in unison. Each puzzle introduces something new. No filler, just problems that will challenge how you think about space and connection. 83 hand-crafted puzzles spread across 7 mechanical zones.",
    features: [
      "83 hand-crafted puzzles across 7 mechanical zones",
      "Rotate and place 3D polyomino pathway pieces",
      "Obstacles, waypoints, teleporters, switches, and multi-ghost coordination",
      "No timers. No hints. Just logic.",
      "16 languages supported including Japanese and Korean",
      "Available on Steam (Windows & macOS) and iOS",
    ],
    platforms: {
      steam: {
        url: "https://store.steampowered.com/app/4085150/Pathways__Poltergeists/",
        appId: "4085150",
      },
      ios: {
        url: "https://apps.apple.com/us/app/pathways-poltergeists/id6752310915",
      },
    },
    trailer: "https://www.youtube.com/watch?v=V5bOVQK9jYY",
    steamAssets: {
      header: "https://shared.fastly.steamstatic.com/store_item_assets/steam/apps/4085150/35be42a71be64497d146ec2414d59b9fbda3e327/header.jpg",
      screenshots: [
        "https://shared.fastly.steamstatic.com/store_item_assets/steam/apps/4085150/17ec48cf7992c758ecaffe19d802a1ff95f6acdd/ss_17ec48cf7992c758ecaffe19d802a1ff95f6acdd.1920x1080.jpg",
      ],
    },
    pressKit: "https://drive.google.com/drive/folders/1xcCn678Z3cP55w8ZprwVwsJbbOwFWfJq",
    tags: ["Puzzle", "Logic", "Strategy", "Difficult", "Building", "Grid-Based Movement", "Isometric", "Stylized", "Supernatural", "Singleplayer", "Atmospheric"],
    featureHighlights: [
      {
        icon: "building",
        title: "Build",
        description: "Rotate and place 3D polyomino pieces to construct pathways",
      },
      {
        icon: "brain",
        title: "Solve",
        description: "83 hand-crafted puzzles across 7 mechanical zones",
      },
      {
        icon: "ghost",
        title: "Guide",
        description: "Navigate ghosts through teleporters, switches, and waypoints",
      },
    ],
    keyMechanics: {
      core: "Players don't control ghosts directly. Instead they rotate and place polyomino pathway pieces onto a grid to construct routes. Ghosts automatically pathfind along connected tiles once a valid path exists.",
      pieces: "I-pieces (straight corridors), L-pieces (90° turns), T-pieces (3-way junctions), S/Z-pieces (offset connections), O-pieces (2×2 hubs). Players rotate before placing; placed pieces are permanent.",
      progression: "Seven mechanical zones introduce new systems: basic pathing → obstacles → waypoints → teleporters → switches/gates → multi-ghost coordination → combined systems.",
    },
    audience: {
      primary: "Women aged 25–32 (validated through professional playtesting with 100% positive response)",
      positioning: "Cozy discovery, honest difficulty — players drawn in by the aesthetic find themselves deeply engaged by the puzzle design",
    },
  },
];

// Helper: get only visible games (exclude unannounced)
export function getVisibleGames(): Game[] {
  return games.filter((g) => g.status !== "unannounced");
}

// Helper: get released games
export function getReleasedGames(): Game[] {
  return games.filter((g) => g.status === "released");
}

// Helper: get a game by slug
export function getGameBySlug(slug: string): Game | undefined {
  return games.find((g) => g.id === slug && g.status !== "unannounced");
}
```

- [ ] **Step 2: Commit**

```bash
git add src/data/games.ts
git commit -m "feat: add typed game catalog with Pathways & Poltergeists data"
```

---

### Task 5: Create SEO Utility

**Files:**
- Create: `src/utils/seo.ts`

- [ ] **Step 1: Create the SEO helper**

```typescript
// src/utils/seo.ts

export interface SEOProps {
  title: string;
  description: string;
  canonicalPath: string;
  ogImage?: string;
  ogType?: string;
  structuredData?: Record<string, unknown>;
}

const SITE_URL = "https://wisegoosegames.com";
const DEFAULT_OG_IMAGE = `${SITE_URL}/og-hero-logo-1200x630.png`;

export function getSEO(props: SEOProps) {
  const fullUrl = `${SITE_URL}${props.canonicalPath}`;
  const ogImage = props.ogImage ?? DEFAULT_OG_IMAGE;
  const ogType = props.ogType ?? "website";

  return {
    title: props.title,
    description: props.description,
    canonical: fullUrl,
    ogType,
    ogUrl: fullUrl,
    ogImage,
    twitterCard: "summary_large_image" as const,
    structuredData: props.structuredData,
  };
}

export function getOrganizationSchema() {
  return {
    "@context": "https://schema.org",
    "@type": "Organization",
    name: "Wise Goose Games",
    url: SITE_URL,
    logo: `${SITE_URL}/logo_512x512.png`,
    description: "Two-person independent game studio based in California, committed to creating thoughtfully designed interactive experiences.",
    email: "contact@wisegoosegames.com",
  };
}

export function getVideoGameSchema(game: {
  title: string;
  shortDescription: string;
  genre: string;
  platforms: Record<string, { url: string }>;
  price: string | null;
  steamAssets: { header?: string };
}) {
  const platformMap: Record<string, string[]> = {
    steam: ["PC", "macOS"],
    ios: ["iOS"],
    android: ["Android"],
  };
  const platformNames = Object.keys(game.platforms).flatMap((p) => platformMap[p] ?? [p]);

  // Extract numeric price from string like "$9.99 USD"
  const priceMatch = game.price?.match(/[\d.]+/);
  const price = priceMatch ? priceMatch[0] : "0";

  return {
    "@context": "https://schema.org",
    "@type": "VideoGame",
    name: game.title,
    description: game.shortDescription,
    genre: game.genre,
    gamePlatform: platformNames,
    applicationCategory: "Game",
    offers: {
      "@type": "Offer",
      price,
      priceCurrency: "USD",
    },
    image: game.steamAssets.header,
    author: {
      "@type": "Organization",
      name: "Wise Goose Games",
    },
  };
}
```

- [ ] **Step 2: Commit**

```bash
git add src/utils/seo.ts
git commit -m "feat: add SEO utility with structured data generators"
```

---

### Task 6: Create BaseLayout

**Files:**
- Create: `src/layouts/BaseLayout.astro`

- [ ] **Step 1: Create the base layout with head, fonts, GA4, ViewTransitions, anti-FOUC script**

```astro
---
// src/layouts/BaseLayout.astro
import { ViewTransitions } from 'astro:transitions';
import '../styles/global.css';

interface Props {
  title: string;
  description: string;
  canonical: string;
  ogType?: string;
  ogUrl: string;
  ogImage: string;
  twitterCard: string;
  structuredData?: Record<string, unknown>;
}

const {
  title,
  description,
  canonical,
  ogType = 'website',
  ogUrl,
  ogImage,
  twitterCard,
  structuredData,
} = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Anti-FOUC: set theme before paint -->
  <script is:inline>
    const t = localStorage.getItem('theme') ??
      (matchMedia('(prefers-color-scheme:dark)').matches ? 'dark' : 'light');
    document.documentElement.setAttribute('data-theme', t);
  </script>

  <title>{title}</title>
  <meta name="description" content={description} />
  <meta name="author" content="Wise Goose Games" />

  <!-- Favicon -->
  <link rel="shortcut icon" type="image/x-icon" href="/favicon.ico" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="icon" type="image/png" href="/favicon.png" />
  <link rel="apple-touch-icon" href="/apple-touch-icon-180x180.png" />
  <meta name="msapplication-TileImage" content="/favicon.png" />

  <!-- Canonical -->
  <link rel="canonical" href={canonical} />

  <!-- Open Graph -->
  <meta property="og:type" content={ogType} />
  <meta property="og:url" content={ogUrl} />
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:image" content={ogImage} />

  <!-- Twitter -->
  <meta property="twitter:card" content={twitterCard} />
  <meta property="twitter:url" content={ogUrl} />
  <meta property="twitter:title" content={title} />
  <meta property="twitter:description" content={description} />
  <meta property="twitter:image" content={ogImage} />

  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&family=Cormorant+Garamond:ital,wght@1,400;1,600&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet" />

  <!-- GA4 -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-VN624MYNHY" is:inline></script>
  <script is:inline>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-VN624MYNHY');
  </script>

  <!-- Structured Data -->
  {structuredData && (
    <script type="application/ld+json" set:html={JSON.stringify(structuredData)} />
  )}

  <ViewTransitions />
</head>
<body>
  <slot />

  <!-- GA4 page view on View Transitions navigation -->
  <script is:inline>
    document.addEventListener('astro:page-load', () => {
      if (typeof gtag !== 'undefined') {
        gtag('event', 'page_view', {
          page_location: window.location.href,
        });
      }
    });
  </script>
</body>
</html>
```

- [ ] **Step 2: Commit**

```bash
git add src/layouts/BaseLayout.astro
git commit -m "feat: add BaseLayout with SEO, GA4, ViewTransitions, anti-FOUC"
```

---

### Task 7: Create GitHub Actions Deploy Workflow

**Files:**
- Create: `.github/workflows/deploy.yml`

- [ ] **Step 1: Create the workflow**

```yaml
# .github/workflows/deploy.yml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist/

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] **Step 2: Commit**

```bash
git add .github/workflows/deploy.yml
git commit -m "feat: add GitHub Actions workflow for Pages deployment"
```

---

### Task 8: Build Verification Checkpoint

- [ ] **Step 1: Create a minimal index page to verify the full pipeline**

Create `src/pages/index.astro`:
```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getSEO, getOrganizationSchema } from '../utils/seo';

const seo = getSEO({
  title: 'Wise Goose Games — Thoughtfully Crafted Games',
  description: 'Wise Goose Games LLC is a two-person independent game studio based in California, committed to creating thoughtfully designed interactive experiences that respect players\' intelligence.',
  canonicalPath: '/',
  structuredData: getOrganizationSchema(),
});
---

<BaseLayout {...seo}>
  <main>
    <h1>Wise Goose Games</h1>
    <p>Site rebuild in progress.</p>
  </main>
</BaseLayout>
```

- [ ] **Step 2: Run build and verify**

```bash
npm run build
```

Expected: Build succeeds. Check `dist/index.html` contains proper `<title>`, meta tags, structured data JSON-LD, GA4 script, anti-FOUC script.

- [ ] **Step 3: Run dev server and verify visually**

```bash
npm run dev
```

Expected: Page loads at `localhost:4321`. Background is warm off-white (`#FDFCF8`). Noise overlay visible on close inspection. Dark mode toggle via browser DevTools (`data-theme="dark"`) switches to warm dark brown (`#1C1612`).

- [ ] **Step 4: Commit**

```bash
git add src/pages/index.astro
git commit -m "feat: add placeholder home page — build pipeline verified"
```

---

## Chunk 2: Shared Components (Footer + Navbar)

### Task 9: Footer Component

**Files:**
- Create: `src/components/Footer.astro`

- [ ] **Step 1: Build the footer**

All static — zero JS. Structure:

```astro
---
// src/components/Footer.astro
---

<footer class="bg-deep-dark text-gray-300 rounded-t-[4rem] mt-20">
  <div class="max-w-6xl mx-auto px-8 py-16 grid grid-cols-1 md:grid-cols-3 gap-12">
    <!-- Column 1: Brand -->
    <div>
      <p class="font-body font-bold text-white text-lg mb-2">Wise Goose Games</p>
      <p class="font-body text-sm text-gray-400">Thoughtfully Crafted Games</p>
    </div>

    <!-- Column 2: Nav Links -->
    <div>
      <p class="font-body font-semibold text-white text-sm uppercase tracking-wider mb-4">Navigate</p>
      <nav class="flex flex-col gap-2">
        <a href="/" class="text-gray-400 hover:text-white text-sm">Home</a>
        <a href="/games" class="text-gray-400 hover:text-white text-sm">Games</a>
        <a href="/contact" class="text-gray-400 hover:text-white text-sm">Contact</a>
      </nav>
    </div>

    <!-- Column 3: Legal -->
    <div>
      <p class="font-body font-semibold text-white text-sm uppercase tracking-wider mb-4">Legal</p>
      <nav class="flex flex-col gap-2">
        <a href="/privacy" class="text-gray-400 hover:text-white text-sm">Privacy Policy</a>
        <a href="/terms" class="text-gray-400 hover:text-white text-sm">Terms of Service</a>
      </nav>
    </div>
  </div>

  <!-- Bottom bar -->
  <div class="border-t border-white/10 max-w-6xl mx-auto px-8 py-6 flex flex-col md:flex-row items-center justify-between gap-4">
    <p class="text-sm text-gray-500">&copy; 2026 Wise Goose Games. All rights reserved.</p>
    <div class="flex items-center gap-2">
      <span class="status-dot"></span>
      <span class="font-mono text-xs text-gray-500">System Operational</span>
    </div>
  </div>
</footer>
```

- [ ] **Step 2: Commit**

```bash
git add src/components/Footer.astro
git commit -m "feat: add Footer component with status indicator"
```

---

### Task 10: Navbar (Astro Shell + React Client Island)

**Files:**
- Create: `src/components/Navbar.astro`
- Create: `src/components/NavbarClient.tsx`

- [ ] **Step 1: Build Navbar.astro — the static shell**

Contains the logo images (goose + both text wordmark variants with light/dark classes), wraps `<NavbarClient client:load />` for the interactive parts.

The Astro file provides the static HTML structure. The React island handles:
- Scroll morphing (IntersectionObserver watching `[data-hero]` element)
- Mobile hamburger menu toggle
- Theme toggle button (sun/moon icon from Lucide)

- [ ] **Step 2: Build NavbarClient.tsx — the interactive island**

```tsx
// src/components/NavbarClient.tsx
import { useEffect, useRef, useState } from 'react';
import { Sun, Moon, Menu, X } from 'lucide-react';

interface Props {
  currentPath: string;
}

const navLinks = [
  { href: '/', label: 'Home' },
  { href: '/games', label: 'Games' },
  { href: '/contact', label: 'Contact' },
];

export default function NavbarClient({ currentPath }: Props) {
  const [isScrolled, setIsScrolled] = useState(false);
  const [isMobileOpen, setIsMobileOpen] = useState(false);
  const [theme, setTheme] = useState<'light' | 'dark'>('light');

  // Theme: read initial from <html> attribute
  useEffect(() => {
    const current = document.documentElement.getAttribute('data-theme') as 'light' | 'dark';
    setTheme(current ?? 'light');
  }, []);

  // Scroll morphing: IntersectionObserver on [data-hero]
  useEffect(() => {
    const hero = document.querySelector('[data-hero]');
    if (!hero) { setIsScrolled(true); return; } // No hero on this page = always scrolled style

    const observer = new IntersectionObserver(
      ([entry]) => setIsScrolled(!entry.isIntersecting),
      { threshold: 0, rootMargin: '-80px 0px 0px 0px' }
    );
    observer.observe(hero);
    return () => observer.disconnect();
  }, []);

  function toggleTheme() {
    const next = theme === 'light' ? 'dark' : 'light';
    setTheme(next);
    document.documentElement.setAttribute('data-theme', next);
    localStorage.setItem('theme', next);
  }

  const scrolledClasses = isScrolled
    ? 'bg-[var(--bg-color)]/80 backdrop-blur-xl border border-[var(--border-color)] shadow-lg'
    : 'bg-transparent border border-transparent';

  const textColor = isScrolled ? 'text-[var(--text-color)]' : 'text-white';

  return (
    <nav
      class={`fixed top-0 left-0 right-0 z-50 mx-auto max-w-5xl mt-4 rounded-full px-6 py-3 transition-all duration-300 ${scrolledClasses}`}
    >
      <div class="flex items-center justify-between">
        {/* Logo area — rendered by Navbar.astro, this component wraps around it */}
        <div class="flex items-center gap-3">
          <a href="/">
            <img src="/WWG_Goose_Header_280px.png" alt="Wise Goose Games" class="h-10 w-auto" />
          </a>
          <a href="/" class="hidden sm:block">
            <img src="/WWG_Text_Header_280px.png" alt="Wise Goose Games" class="logo-text-light h-8 w-auto" />
            <img src="/WWG_Text_Header_Dark_280px.png" alt="Wise Goose Games" class="logo-text-dark h-8 w-auto" />
          </a>
        </div>

        {/* Desktop nav */}
        <div class={`hidden md:flex items-center gap-1 ${textColor}`}>
          {navLinks.map((link) => (
            <a
              key={link.href}
              href={link.href}
              class={`px-4 py-2 rounded-full text-sm font-semibold transition-colors ${
                currentPath === link.href ? 'bg-[var(--accent-color)] text-white' : 'hover:bg-white/10'
              }`}
            >
              {link.label}
            </a>
          ))}
          <button onClick={toggleTheme} class="ml-2 p-2 rounded-full hover:bg-white/10 transition-colors">
            {theme === 'dark' ? <Sun size={18} /> : <Moon size={18} />}
          </button>
        </div>

        {/* Mobile hamburger */}
        <button class={`md:hidden p-2 ${textColor}`} onClick={() => setIsMobileOpen(!isMobileOpen)}>
          {isMobileOpen ? <X size={24} /> : <Menu size={24} />}
        </button>
      </div>

      {/* Mobile dropdown */}
      {isMobileOpen && (
        <div class="md:hidden mt-4 pb-4 flex flex-col gap-2">
          {navLinks.map((link) => (
            <a
              key={link.href}
              href={link.href}
              class="px-4 py-3 rounded-xl text-sm font-semibold text-[var(--text-color)] hover:bg-[var(--light-gray)] transition-colors"
              onClick={() => setIsMobileOpen(false)}
            >
              {link.label}
            </a>
          ))}
          <button onClick={toggleTheme} class="px-4 py-3 rounded-xl text-sm font-semibold text-[var(--text-color)] hover:bg-[var(--light-gray)] text-left flex items-center gap-2">
            {theme === 'dark' ? <Sun size={16} /> : <Moon size={16} />}
            {theme === 'dark' ? 'Light Mode' : 'Dark Mode'}
          </button>
        </div>
      )}
    </nav>
  );
}
```

**Note:** The `currentPath` prop is passed from `Navbar.astro` via `Astro.url.pathname`. The logo images are rendered inside the React island (not in the Astro shell) so they participate in the text color transitions.

- [ ] **Step 3: Commit**

```bash
git add src/components/Navbar.astro src/components/NavbarClient.tsx
git commit -m "feat: add Navbar with scroll morphing, mobile menu, theme toggle"
```

---

### Task 11: Wire Shared Components into Layout

**Files:**
- Modify: `src/layouts/BaseLayout.astro`

- [ ] **Step 1: Import and add Navbar and Footer to BaseLayout**

Add `<Navbar />` before `<slot />` and `<Footer />` after `<slot />` in `BaseLayout.astro`.

- [ ] **Step 2: Verify navbar morphing works**

```bash
npm run dev
```

Expected: Navbar renders as floating pill. Scrolling past hero triggers morph to blurred background. Theme toggle switches light/dark. Mobile hamburger works on narrow viewport.

- [ ] **Step 3: Commit**

```bash
git add src/layouts/BaseLayout.astro
git commit -m "feat: wire Navbar and Footer into BaseLayout"
```

---

## Chunk 3: Home Page

### Task 12: Hero Component

**Files:**
- Create: `src/components/Hero.tsx`

- [ ] **Step 1: Build Hero.tsx**

```tsx
// src/components/Hero.tsx
import { useEffect, useRef } from 'react';
import gsap from 'gsap';

interface Props {
  steamUrl: string;
  headerImage: string;
}

export default function Hero({ steamUrl, headerImage }: Props) {
  const containerRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    if (prefersReducedMotion) {
      gsap.set(containerRef.current?.querySelectorAll('[data-animate]') ?? [], {
        opacity: 1, y: 0, clearProps: 'all',
      });
      return;
    }

    const ctx = gsap.context(() => {
      gsap.from('[data-animate]', {
        y: 40,
        opacity: 0,
        duration: 0.8,
        ease: 'power3.out',
        stagger: 0.08,
      });
    }, containerRef);
    return () => ctx.revert();
  }, []);

  return (
    <section
      ref={containerRef}
      data-hero
      class="relative min-h-dvh flex items-end overflow-hidden"
    >
      {/* Background image */}
      <div
        class="absolute inset-0 bg-cover bg-center"
        style={{ backgroundImage: `url(${headerImage})` }}
      />
      {/* Gradient overlay */}
      <div class="absolute inset-0 bg-gradient-to-t from-[var(--bg-color)] via-[var(--bg-color)]/60 to-transparent" />

      {/* Content — bottom-left */}
      <div class="relative z-10 max-w-6xl mx-auto px-6 pb-24 md:pb-32 w-full">
        <p data-animate class="font-body font-semibold tracking-tight text-3xl md:text-5xl text-white mb-2">
          Thoughtfully crafted
        </p>
        <p data-animate class="font-drama italic text-6xl md:text-8xl text-white">
          Games<span class="text-accent">.</span>
        </p>
        <p data-animate class="font-body text-lg text-white/80 mt-6 mb-8">
          Independent studio. Handmade puzzles. No filler.
        </p>
        <a data-animate href={steamUrl} target="_blank" rel="noopener noreferrer" class="btn-accent">
          <span class="bg-slide" />
          Play Pathways &amp; Poltergeists
        </a>
      </div>
    </section>
  );
}
```

**Key:** The `data-hero` attribute on the section is what the Navbar's IntersectionObserver watches.

- [ ] **Step 2: Commit**

```bash
git add src/components/Hero.tsx
git commit -m "feat: add Hero component with GSAP staggered entrance"
```

---

### Task 13: FeaturedGame Component

**Files:**
- Create: `src/components/FeaturedGame.tsx`

- [ ] **Step 1: Build FeaturedGame.tsx**

Accepts the game object as a prop. Renders:

1. **Steam Header Card** — `<img>` of `steamAssets.header`, `rounded-2xl`, subtle drop shadow
2. **Info Panel** — Title, tagline badge, short description, platform icons (Steam icon + Apple icon from Lucide), price badge
3. **Trailer Embed** — YouTube iframe, `rounded-2xl`, responsive 16:9 via `aspect-video` class. Extract video ID from `trailer` URL for embed URL: `https://www.youtube.com/embed/{videoId}`
4. **Feature Highlight Cards** — Map over `featureHighlights` array. Each card:
   - Lucide icon resolved via a lookup map (Lucide doesn't support dynamic string imports):
     ```tsx
     import { Building, Brain, Ghost, Puzzle, Gamepad2 } from 'lucide-react';
     const iconMap: Record<string, React.ComponentType<{ size?: number }>> = {
       building: Building, brain: Brain, ghost: Ghost, puzzle: Puzzle, gamepad2: Gamepad2,
     };
     const Icon = iconMap[highlight.icon] ?? Puzzle;
     ```
   - Title in `font-semibold`
   - Description text
   - `rounded-2xl bg-surface border border-border` with hover lift
5. **Buy CTAs** — Steam button + App Store button, both `btn-accent` variant, with platform icons

**GSAP (scroll-triggered, `client:visible`):**
- Cards stagger in: `y: 60 → 0, opacity: 0 → 1, stagger: 0.15`
- ScrollTrigger: `start: "top 80%"`, `once: true`
- Same reduced-motion guard

- [ ] **Step 2: Commit**

```bash
git add src/components/FeaturedGame.tsx
git commit -m "feat: add FeaturedGame showcase with trailer and feature cards"
```

---

### Task 14: Philosophy Component

**Files:**
- Create: `src/components/Philosophy.tsx`

- [ ] **Step 1: Build Philosophy.tsx**

Full-width section:
- Background: `bg-light-gray` — since CSS variables already swap per theme, `bg-light-gray` resolves to `#F0F0F0` in light mode and `#2D2621` in dark mode. No `dark:` variant needed.
- Padding: generous vertical (py-24 md:py-32)

Typography contrast:
- First block: "Most puzzle games focus on:" followed by "quantity, procedural generation, difficulty for its own sake." — `font-body text-lg text-text/60`
- Second block: "We focus on:" followed by "hand-crafted challenges that respect how you think." — `font-drama italic text-4xl md:text-6xl` with "hand-crafted" wrapped in `<span class="text-accent">`

**GSAP (scroll-triggered):**
- Word-by-word or line-by-line fade-up
- Split text into `<span>` elements per word/line
- Each word: `y: 20 → 0, opacity: 0 → 1, stagger: 0.03`
- ScrollTrigger: `start: "top 75%"`, `once: true`
- Reduced-motion guard

- [ ] **Step 2: Commit**

```bash
git add src/components/Philosophy.tsx
git commit -m "feat: add Philosophy section with scroll-triggered text animation"
```

---

### Task 15: Studio Component

**Files:**
- Create: `src/components/Studio.astro`

- [ ] **Step 1: Build Studio.astro**

```astro
---
// src/components/Studio.astro
---

<section class="py-20 md:py-28 px-4">
  <div class="max-w-2xl mx-auto text-center">
    <h2 class="font-body font-bold text-3xl md:text-4xl mb-6">Who We Are</h2>
    <p class="font-body text-lg text-[var(--text-color)]/80 leading-relaxed mb-4">
      Wise Goose Games is a two-person indie studio based in California.
    </p>
    <p class="font-body text-lg text-[var(--text-color)]/80 leading-relaxed">
      We make games because we believe puzzles should feel like conversations — not chores.
      Every mechanic is placed with intention. Every level is hand-crafted.
      No procedural filler, no artificial padding — just honest design.
    </p>
  </div>
</section>
```

- [ ] **Step 2: Commit**

```bash
git add src/components/Studio.astro
git commit -m "feat: add Studio section component"
```

---

### Task 16: Assemble Home Page

**Files:**
- Modify: `src/pages/index.astro`

- [ ] **Step 1: Replace placeholder with full home page**

Wire up all components in order:
1. `<Hero client:load />` — with `data-hero` attribute for navbar
2. `<FeaturedGame client:visible game={games[0]} />`
3. `<Philosophy client:visible />`
4. `<Studio />`

Import game data from `src/data/games.ts`. Use the first released game for the featured section.

- [ ] **Step 2: Verify home page renders**

```bash
npm run dev
```

Expected: Full home page with hero, featured game, philosophy, studio sections. GSAP animations trigger on scroll. Dark/light mode works throughout.

- [ ] **Step 3: Commit**

```bash
git add src/pages/index.astro
git commit -m "feat: assemble home page with all sections"
```

---

## Chunk 4: Games Pages

### Task 17: GameCard Component

**Files:**
- Create: `src/components/GameCard.astro`

- [ ] **Step 1: Build GameCard.astro**

Static card component. Props: `game: Game`.

Layout:
- `rounded-2xl bg-surface border border-border overflow-hidden` with hover lift (`hover:-translate-y-1`)
- Header art image: `aspect-video object-cover w-full`
- Content area: padded
  - Title: `font-semibold text-xl`
  - Tagline: `text-text/70`
  - Status badge: colored chip (green for released, amber for coming-soon, blue for announced)
  - Platform icons row
  - CTA button: "Play Now" (released) → links to first platform URL. "Wishlist" (coming-soon/announced) → links to Steam or first platform.

- [ ] **Step 2: Commit**

```bash
git add src/components/GameCard.astro
git commit -m "feat: add GameCard component for catalog grid"
```

---

### Task 18: Games Catalog Page

**Files:**
- Create: `src/pages/games/index.astro`

- [ ] **Step 1: Build the catalog page**

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import GameCard from '../../components/GameCard.astro';
import { getVisibleGames } from '../../data/games';
import { getSEO } from '../../utils/seo';

const seo = getSEO({
  title: 'Games — Wise Goose Games',
  description: 'Explore our catalog of thoughtfully crafted games from Wise Goose Games.',
  canonicalPath: '/games',
});

const visibleGames = getVisibleGames();
---

<BaseLayout {...seo}>
  <main class="max-w-6xl mx-auto px-4 pt-32 pb-20">
    <h1 class="font-body font-bold text-4xl md:text-5xl text-center mb-4">Our Games</h1>
    <p class="font-body text-lg text-text/70 text-center max-w-xl mx-auto mb-12">
      Thoughtfully crafted interactive experiences. No filler.
    </p>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {visibleGames.map((game) => (
        <a href={`/games/${game.id}`} class="no-underline">
          <GameCard game={game} />
        </a>
      ))}
    </div>

    {visibleGames.length <= 1 && (
      <p class="text-center text-text/50 font-mono text-sm mt-12">
        More games coming soon.
      </p>
    )}
  </main>
</BaseLayout>
```

- [ ] **Step 2: Verify**

```bash
npm run dev
```

Navigate to `/games`. Expected: Grid with Pathways & Poltergeists card. "More games coming soon" note below.

- [ ] **Step 3: Commit**

```bash
git add src/pages/games/index.astro
git commit -m "feat: add Games catalog page"
```

---

### Task 19: GameDetail Component

**Files:**
- Create: `src/components/GameDetail.tsx`

- [ ] **Step 1: Build GameDetail.tsx**

React island receiving `game: Game` as prop. Sections:

1. **Hero Banner** — Full-width header art with title overlay, gradient from bottom
2. **Trailer** — YouTube embed in `aspect-video rounded-2xl overflow-hidden`
3. **Description** — `longDescription` rendered as paragraphs (split on `\n`)
4. **Feature Highlights** — 3-card grid from `featureHighlights`
5. **Screenshots Gallery** — Grid of screenshot images with lightbox:
   ```tsx
   // Lightbox state
   const [lightboxIndex, setLightboxIndex] = useState<number | null>(null);
   const screenshots = game.steamAssets.screenshots ?? [];

   // Keyboard handler (attach via useEffect when open)
   useEffect(() => {
     if (lightboxIndex === null) return;
     const handler = (e: KeyboardEvent) => {
       if (e.key === 'Escape') setLightboxIndex(null);
       if (e.key === 'ArrowRight') setLightboxIndex((i) => i !== null ? Math.min(i + 1, screenshots.length - 1) : null);
       if (e.key === 'ArrowLeft') setLightboxIndex((i) => i !== null ? Math.max(i - 1, 0) : null);
     };
     window.addEventListener('keydown', handler);
     return () => window.removeEventListener('keydown', handler);
   }, [lightboxIndex]);

   // Lightbox JSX: fixed inset-0 z-50 bg-black/90 flex items-center justify-center
   // Click backdrop → close. Left/Right arrow buttons on sides.
   // Image: max-w-[90vw] max-h-[90vh] object-contain rounded-xl
   ```
6. **Buy CTAs** — Platform buttons (Steam, App Store)
7. **Press Kit** — Link to `pressKit` URL if present: "Press Kit →"

**GSAP (scroll-triggered):**
- Sections stagger in as user scrolls
- Same pattern: `gsap.context` + `ScrollTrigger` + reduced-motion guard

- [ ] **Step 2: Commit**

```bash
git add src/components/GameDetail.tsx
git commit -m "feat: add GameDetail component with lightbox gallery"
```

---

### Task 20: Game Detail Page (Dynamic Route)

**Files:**
- Create: `src/pages/games/[slug].astro`

- [ ] **Step 1: Build the dynamic route**

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import GameDetail from '../../components/GameDetail.tsx';
import { getVisibleGames, getGameBySlug } from '../../data/games';
import { getSEO, getVideoGameSchema } from '../../utils/seo';

export function getStaticPaths() {
  const games = getVisibleGames();
  return games.map((game) => ({
    params: { slug: game.id },
    props: { game },
  }));
}

const { game } = Astro.props;

const seo = getSEO({
  title: `${game.title} — Wise Goose Games`,
  description: game.shortDescription,
  canonicalPath: `/games/${game.id}`,
  ogImage: game.steamAssets.header,
  structuredData: getVideoGameSchema(game),
});
---

<BaseLayout {...seo}>
  <main>
    <GameDetail client:visible game={game} />
  </main>
</BaseLayout>
```

- [ ] **Step 2: Verify**

```bash
npm run dev
```

Navigate to `/games/pathways-poltergeists`. Expected: Full game detail page with hero, trailer, description, feature cards, screenshots, buy CTAs, press kit link.

- [ ] **Step 3: Commit**

```bash
git add src/pages/games/\[slug\].astro
git commit -m "feat: add dynamic game detail pages from catalog data"
```

---

## Chunk 5: Remaining Pages

### Task 21: Contact Page

**Files:**
- Create: `src/components/ContactForm.tsx`
- Create: `src/pages/contact.astro`

- [ ] **Step 1: Build ContactForm.tsx**

React island with form state management.

**Formspree integration:**
- Form ID from existing site: `mwpnzqvw`
- Submit via `fetch('https://formspree.io/f/mwpnzqvw', { method: 'POST', body: FormData, headers: { 'Accept': 'application/json' } })`

**Fields:**
- Name (required)
- Email (required, email format validation)
- Subject (optional)
- Message (required)
- Honeypot: hidden `_gotcha` field for spam prevention

**Validation:**
- On blur: validate field, show inline error below
- Email: regex check for basic format
- Required fields: show "This field is required"

**States:**
- `idle`: form editable, "Send Message" button
- `submitting`: button shows spinner (Lucide `Loader2` with `animate-spin`), all fields disabled
- `success`: form replaced with "Message sent! We'll get back to you soon." in green
- `error`: error banner above form: "Something went wrong. Please email us directly at contact@wisegoosegames.com."

**Styling:**
- Inputs: `rounded-lg border border-border bg-surface px-4 py-3 focus:border-accent focus:outline-none`
- Labels: `font-semibold text-sm`
- Card container: `rounded-2xl bg-surface border border-border p-8 md:p-10 max-w-xl mx-auto`

- [ ] **Step 2: Build contact.astro**

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import ContactForm from '../components/ContactForm.tsx';
import { getSEO } from '../utils/seo';

const seo = getSEO({
  title: 'Contact — Wise Goose Games',
  description: 'Get in touch with Wise Goose Games for press inquiries, business opportunities, or general questions.',
  canonicalPath: '/contact',
});
---

<BaseLayout {...seo}>
  <main class="max-w-6xl mx-auto px-4 pt-32 pb-20">
    <h1 class="font-body font-bold text-4xl md:text-5xl text-center mb-4">Get in Touch</h1>
    <p class="font-body text-lg text-text/70 text-center max-w-xl mx-auto mb-4">
      Whether you have questions about our games, press inquiries, or just want to say hello — we'd love to hear from you.
    </p>
    <p class="font-body text-sm text-accent text-center mb-12">
      For press inquiries, please include your outlet name and deadline.
    </p>

    <ContactForm client:visible />
  </main>
</BaseLayout>
```

- [ ] **Step 3: Verify form works**

```bash
npm run dev
```

Navigate to `/contact`. Expected: Form renders, validation works on blur, submit sends to Formspree (test with a real submission or check network tab).

- [ ] **Step 4: Commit**

```bash
git add src/components/ContactForm.tsx src/pages/contact.astro
git commit -m "feat: add Contact page with Formspree form"
```

---

### Task 22: Privacy Policy Page

**Files:**
- Create: `src/pages/privacy.astro`

- [ ] **Step 1: Migrate privacy content from existing HTML**

Create `src/pages/privacy.astro`. Import `BaseLayout`. Wrap the existing privacy policy content (from `privacy.html` body) in the layout. Convert HTML to Astro template syntax. Use Tailwind classes for styling:
- Container: `max-w-3xl mx-auto px-4 pt-32 pb-20`
- Content card: `rounded-2xl bg-surface border border-border p-8 md:p-10`
- Headings: `font-body font-bold text-2xl mb-4`
- Subheadings: `font-body font-semibold text-lg mt-8 mb-2`
- Body: `font-body text-text/80 leading-relaxed`
- Links: `text-accent hover:underline`
- Last updated badge: `rounded-lg bg-light-gray px-4 py-3 text-sm italic mb-8`

Content to migrate: the full privacy policy text from `privacy.html` lines 67–196 (all the `<div class="privacy-section">` blocks).

- [ ] **Step 2: Commit**

```bash
git add src/pages/privacy.astro
git commit -m "feat: add Privacy Policy page (migrated from static HTML)"
```

---

### Task 23: Terms of Service Page

**Files:**
- Create: `src/pages/terms.astro`

- [ ] **Step 1: Migrate terms content from existing HTML**

Same pattern as privacy page. Migrate the content from `terms.html` lines 66–239.

- [ ] **Step 2: Commit**

```bash
git add src/pages/terms.astro
git commit -m "feat: add Terms of Service page (migrated from static HTML)"
```

---

### Task 24: 404 Page

**Files:**
- Create: `src/pages/404.astro`

- [ ] **Step 1: Create a branded 404 page**

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getSEO } from '../utils/seo';

const seo = getSEO({
  title: 'Page Not Found — Wise Goose Games',
  description: 'The page you are looking for does not exist.',
  canonicalPath: '/404',
});
---

<BaseLayout {...seo}>
  <main class="max-w-6xl mx-auto px-4 pt-32 pb-20 text-center min-h-[60vh] flex flex-col items-center justify-center">
    <h1 class="font-drama italic text-8xl md:text-9xl text-accent mb-4">404</h1>
    <p class="font-body text-xl text-text/70 mb-8">This page wandered off into the fog.</p>
    <a href="/" class="btn-accent">
      <span class="bg-slide"></span>
      Back to Home
    </a>
  </main>
</BaseLayout>
```

- [ ] **Step 2: Commit**

```bash
git add src/pages/404.astro
git commit -m "feat: add branded 404 page"
```

---

## Chunk 6: Cleanup & Final Verification

### Task 25: Remove Old Static Files

**Files:**
- Delete: `index.html`, `games.html`, `contact.html`, `privacy.html`, `terms.html`
- Delete: `styles.css`, `script.js`
- Delete: `logo-goose-140px.png`, `logo-text-140px.png` (retired)
- Delete: `sitemap.xml` (now auto-generated)
- Delete: Root copies of all assets now in `public/` (logos, favicons, CNAME, etc.)
- Keep at root: `README.md`

**Note:** During Tasks 1–24, the old root-level HTML/CSS/JS files coexist harmlessly with the new Astro project in `src/`. Astro reads only from `src/` and `public/`. The old files are inert and don't interfere with builds.

**IMPORTANT:** Before deleting, verify the old files' content has been fully migrated. Do NOT delete until all pages build successfully.

- [ ] **Step 1: Verify full build succeeds**

```bash
npm run build
```

Expected: All pages built. Check `dist/` contains: `index.html`, `games/index.html`, `games/pathways-poltergeists/index.html`, `contact/index.html`, `privacy/index.html`, `terms/index.html`, `404.html`, `sitemap-index.xml`, all static assets.

- [ ] **Step 2: Delete old files**

```bash
rm index.html games.html contact.html privacy.html terms.html
rm styles.css script.js
rm logo-goose-140px.png logo-text-140px.png
rm -f sitemap.xml
```

- [ ] **Step 3: Move remaining root assets that should be in public/**

Any root-level assets that were already copied to `public/` can be removed from root:
```bash
# These should already be in public/ from Task 2
rm WWG_Goose_Header_280px.png WWG_Text_Header_280px.png WWG_Text_Header_Dark_280px.png
rm favicon.ico favicon.png favicon.svg
rm apple-touch-icon-180x180.png og-hero-logo-1200x630.png logo_512x512.png
rm robots.txt app-ads.txt pinterest-8e7d0.html
rm .nojekyll CNAME
rm -rf .well-known
```

- [ ] **Step 4: Verify build still succeeds after cleanup**

```bash
npm run build
```

- [ ] **Step 5: Verify no unintended files before committing**

```bash
git status
```

Review the output. Only old HTML/CSS/JS files should show as deleted, and no unexpected files should be staged.

- [ ] **Step 6: Commit**

```bash
git add index.html games.html contact.html privacy.html terms.html styles.css script.js logo-goose-140px.png logo-text-140px.png sitemap.xml WWG_Goose_Header_280px.png WWG_Text_Header_280px.png WWG_Text_Header_Dark_280px.png favicon.ico favicon.png favicon.svg apple-touch-icon-180x180.png og-hero-logo-1200x630.png logo_512x512.png robots.txt app-ads.txt pinterest-8e7d0.html .nojekyll CNAME .well-known/
git commit -m "chore: remove old static HTML files, complete migration to Astro"
```

---

### Task 26: Full Verification

- [ ] **Step 1: Build and preview**

```bash
npm run build && npm run preview
```

- [ ] **Step 2: Verify all pages render correctly**

Check each route:
- `/` — Hero, featured game, philosophy, studio sections all render
- `/games` — Catalog grid with P&P card, "More games coming soon" note
- `/games/pathways-poltergeists` — Full detail page with trailer, features, screenshots, buy CTAs
- `/contact` — Form renders, validation works, Formspree submission works
- `/privacy` — Full privacy policy content
- `/terms` — Full terms content
- `/404` — Custom 404 with branded styling

- [ ] **Step 3: Verify dark/light mode on every page**

Toggle theme. Check:
- All backgrounds use warm brown tones in dark mode (not neutral grays)
- Logo text wordmark swaps correctly
- Navbar morph looks correct in both themes
- All text is readable in both modes

- [ ] **Step 4: Verify responsive on mobile viewport**

Check at 375px width:
- Navbar collapses to hamburger
- Hero text scales down
- Cards stack vertically
- Footer stacks vertically
- Contact form is full-width

- [ ] **Step 5: Verify all external links**

Check these URLs resolve:
- Steam: `https://store.steampowered.com/app/4085150/Pathways__Poltergeists/`
- App Store: `https://apps.apple.com/us/app/pathways-poltergeists/id6752310915`
- YouTube: `https://www.youtube.com/watch?v=V5bOVQK9jYY`
- Press Kit: `https://drive.google.com/drive/folders/1xcCn678Z3cP55w8ZprwVwsJbbOwFWfJq`

- [ ] **Step 6: Verify SEO**

Check `dist/index.html`:
- `<title>` tag present
- `<meta name="description">` present
- OG and Twitter meta tags present
- Canonical URL present
- JSON-LD structured data present

Check `dist/games/pathways-poltergeists/index.html`:
- VideoGame schema in JSON-LD
- Game-specific OG image (Steam header)

Check `dist/sitemap-index.xml` exists and lists all routes.

- [ ] **Step 7: Commit final state**

```bash
git add -A
git commit -m "chore: final verification — all pages, themes, responsive, SEO confirmed"
```
